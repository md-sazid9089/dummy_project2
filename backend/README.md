# Backend

Express and Next.js server for the Bachelor Solution project.

## Development

```bash
npm install
npm run dev
```

## Health Check

A simple health check endpoint is available at `/api/health` which responds with `{ "status": "ok" }`.
